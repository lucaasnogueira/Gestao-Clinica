# Usado pelo docker-compose.yml
# Variáveis do serviço "web"

# Acessado pelo browser (host externo)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Acessado pelo servidor Next.js (interno Docker)
API_URL=http://api:3001/api/v1

NODE_ENV=production
